# Optional show name overrides consumed by sync_shows.sh.
# Format per line: Original Name = Canonical Name
# Leading/trailing whitespace is ignored. Lines beginning with # are comments.
# Use this to pin tricky series naming even after automatic year detection.
#
# Example:
# Gen V = Gen V (2023)
